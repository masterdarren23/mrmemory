# 2026-03-24

## AMR 4 Feature Roadmap (from critic analysis)
Dirwood defined 4 large integrations for AMR based on critic feedback:

1. **LLM auto_remember** — `POST /v1/memories/auto` — ✅ DONE & deployed on Fly.io
   - Async + sync modes, dedup, BYOK support, GPT-4o-mini extraction
2. **Graph retrieval** — Entity/relationship extraction, knowledge graph — ❌ NOT STARTED (lowest priority, deferred to Postgres entity tables)
3. **LangChain checkpointer** — `MrMemoryCheckpointer` + `MrMemoryStore` — ✅ CODE BUILT, needs SDK republish to PyPI
4. **Compression engine** — Memory summarization/consolidation via LLM — ❌ NOT STARTED

Priority order: #3 → #1 → #4 → #2 (features #1,#3,#4 share LLM pipeline)

## Session Issues
- Gateway died due to API billing error (Anthropic credits ran out) around 13:45-14:08 PDT
- Multiple billing errors before crash, not a scheduled task failure
- Dirwood topped up credits and did /reset to recover
- Telegram chat export located at: `C:\Users\johnl\OneDrive\Desktop\ChatExport_2026-03-24 (1) - Copy`

## Completed This Session
- Feature #3 LangChain checkpointer: published in mrmemory 0.3.0 on PyPI
- Feature #4 Compression engine: `POST /v1/memories/compress` built in `api/src/routes/compress.rs`, deployed to Fly.io
- Python SDK 0.3.1 published (adds `compress()` method)
- SDK README updated with auto_remember, compress, LangChain examples
- Docs page updated & rebranded to "MrMemory Documentation"
- Landing page rebrand: added purple brain logo to nav, `amr.dev` → `mrmemory` branding, all "AMR" → "MrMemory" in copy, tagline updated
- Logo file: `landing/logo.jpg` (purple neon brain circuit icon)
- All changes pushed to GitHub master, auto-deploying to Vercel (mrmemory.dev)

## Still TODO
- TypeScript SDK (`memorymr` on npm): needs `autoRemember()` and `compress()` methods added, then republish
- Feature #2 Graph retrieval: deferred (Postgres entity tables approach planned for v2)
- Marketing: post launch content (Show HN, Reddit r/LangChain, tweets) — content drafted but needs manual posting
