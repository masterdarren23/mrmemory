# 2026-03-17 — Session Notes

## SDKs Published
- **Python SDK**: `pip install mrmemory` — live on PyPI (name "amr" was taken)
  - PyPI username: masterdarren23
- **TypeScript SDK**: `npm install memorymr` — live on npm (name "mrmemory" was taken on npm too)
  - npm username: fawnboy13
  - Required 2FA OTP for publishing — timing issues through remote exec, Dirwood published manually

## GitHub Repo
- https://github.com/masterdarren23/mrmemory
- Connected to Vercel for auto-deploy
- Had to remove .npmrc with npm token (GitHub secret scanning blocked push)
- Git config: user "Mr. Dogood" <mrdogood@amr.dev>

## Vercel/Landing Page
- Root directory: `amr-project/landing`
- mrmemory.dev domain moved from old "amr" project to new "mrmemory" project
- docs.html 404 issue fixed by moving to `docs/index.html` (directory-based routing)
- vercel.json at repo root with rewrites (didn't help, directory approach worked)
- Stripe payment link buttons updated: https://buy.stripe.com/9B6eVf0Jw0GefiP36l8g000
- $25 tier removed (kept looping with $5), just $5/mo for now

## Post-Payment Signup Flow Built
- New files: `api/src/routes/billing.rs`, migration, STRIPE-SETUP.md
- Stripe webhook at POST /v1/billing/webhook (HMAC-SHA256 verification)
- On checkout.session.completed: auto-creates tenant + API key
- Welcome page at GET /v1/welcome?session_id=... shows API key with copy button
- Falls back to Stripe API fetch if webhook hasn't fired yet
- Config updated with stripe_webhook_secret and stripe_secret_key

## Auth Refactor (Late Session)
- Converted all `sqlx::query!` compile-time macros → runtime `sqlx::query` across db/keys.rs, db/memories.rs, auth/mod.rs, routes/billing.rs
- Fixed Dockerfile: removed dummy main.rs caching trick that produced empty binary
- Rewrote auth/mod.rs: `TenantContext` no longer implements `FromRequestParts` (lifetime/Box::pin issue with Rust ≥1.88 + axum-core 0.4.5)
- Now uses standalone `extract_tenant(parts, state)` async function
- **BLOCKING**: Route handlers in routes/memories.rs (and possibly others) still use `tenant: TenantContext` as extractor parameter — need updating to call `extract_tenant()` manually
- Fly secrets all set: DATABASE_URL, OPENAI_API_KEY, QDRANT_URL, STRIPE_PRICE_ID, STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, RUST_BACKTRACE=1
- Orphaned Fly machine `3d8d5517f95768` needs destroying

## Still Needed to Deploy
- Update route handlers to use `extract_tenant()` instead of extractor
- `fly deploy --no-cache` from amr-project/api
- Create Stripe webhook endpoint in dashboard pointing to /v1/billing/webhook
- Dirwood already set the after-payment redirect URL

## 🎉 AMR FULLY LIVE — 2026-03-17 6:27 PM PDT
- Full signup flow working: Stripe payment → webhook → tenant provisioned → welcome page with API key
- Backend healthy on Fly.io (machine 3d8d5519b13738)
- Stripe webhook created and signing secret set
- Dirwood tested with real card — successfully subscribed and unsubscribed
- Fixed issues: missing migration 20260315000000 (deleted from DB), idempotent migrations (IF NOT EXISTS), crashed machines (destroyed and recreated)

## API Credits
- Anthropic API hit billing error mid-sub-agent run, but Dirwood says $19 left
- Sub-agent actually completed all work despite the error message
