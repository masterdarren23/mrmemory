# 2026-03-18 — Session Notes

## AMR Backend Finally Live (from yesterday evening)
- Fixed migration crash loop: deleted phantom migration 20260315000000 from _sqlx_migrations table
- Made all migrations idempotent (IF NOT EXISTS / IF NOT EXISTS)
- Had to destroy crashed machines and redeploy fresh
- Health endpoint confirmed working: `{"status":"ok","version":"0.1.0"}`

## Stripe Webhook Created
- Webhook endpoint added in Stripe dashboard
- URL: https://amr-memory-api.fly.dev/v1/billing/webhook
- Webhook signing secret: set as Fly secret (whsec_NXncPEcdjizT8dDCS3g7DookdM8SDVQK)
- **Full payment flow tested LIVE** — Dirwood paid with real card, got welcome page with API key
- Stripe customer portal works for cancellation

## Landing Page Fixes
- Fixed docs links: changed all `docs.html` → `/docs` (4 occurrences)
- Updated code example to show full trading-bot example with agent_id, namespace, tags, and recall loop
- All pushed to GitHub, auto-deployed via Vercel

## WebSocket Real-Time Memory Sharing — BUILT
- Dirwood's key differentiator vs mem0: real-time multi-agent memory sharing
- Sub-agent built the full WebSocket layer in ~3 minutes:
  - src/ws/mod.rs — WsManager with tokio broadcast, client registry, subscriptions
  - src/routes/ws.rs — /v1/ws?token=amr_sk_... endpoint with auth
  - migrations/20260318000000_memory_shares.sql — ACL table
  - Updated state.rs, memories.rs (broadcast on create/delete), main.rs, Cargo.toml
- Compiles locally with cargo check ✅

## WebSocket Deploy — BLOCKED
- Same migration pattern: memory_shares table already exists in DB but migration record missing
- Fixed migration to be idempotent (CREATE TABLE IF NOT EXISTS)
- Need to: delete migration record from DB, then redeploy
- Machine 3d8d5519b13738 hit max restart count again
- Gave Dirwood the commands to run

## Dirwood's Strategy
- Using separate agent swarm for marketing push
- WebSocket feature is THE differentiator — "connective tissue of multi-agent organisms"
- Quote: "These create very strong product stickiness... could become the default shared brain for agent teams in 2026-2027"
- Plan: get WebSocket working → update docs/landing → mass marketing

## WebSocket Deploy — SUCCEEDED 🎉
- After multiple attempts: dropped memory_shares table from correct DB (amr_memory_api, not postgres)
- Had to use `fly postgres connect --app amr-db --database amr_memory_api` (default connects to wrong DB!)
- Machine 6e82ee71bd4458 then 1781337dbd10e8 then final working deploy
- All endpoints confirmed working:
  - Health ✅, Auth ✅, Create memory ✅, List memories ✅
  - Test API key created: amr_sk_test_live_key_001 (tenant: 93d77e31-c5fa-40f8-b19d-c39fa274dcb0)
  - WebSocket endpoint live at wss://amr-memory-api.fly.dev/v1/ws?token=...
- Dirwood planning to use separate agent swarm for marketing push

## Three Stubs Fixed (Late Session)
- **Recall route**: Now uses real Qdrant vector search + OpenAI embeddings (was returning hardcoded 0.85)
  - create_memory spawns background embedding generation + Qdrant upsert
  - recall_memories: query → OpenAI embedding → Qdrant search → Postgres hydration
- **Auth/create_key route**: Wired to DB via db/keys.rs (was a stub)
- **Health endpoint**: Real Postgres + Qdrant pings, removed fake redis stub
- Code reviewer agent confirmed: no bugs, no security issues, compiles clean
- **AWAITING DEPLOY** — Dirwood needs to run `fly deploy --no-cache`

## Key DB Info
- Postgres database name: `amr_memory_api` (NOT `postgres` — this tripped us up multiple times)
- Must use `fly postgres connect --app amr-db --database amr_memory_api` to connect to correct DB
- Test tenant: 93d77e31-c5fa-40f8-b19d-c39fa274dcb0, key: amr_sk_test_live_key_001

## Recurring Deploy Issue Pattern
- Every new migration crashes because table/column already exists (from prior deleted migrations)
- Fix: always write migrations with IF NOT EXISTS / IF NOT EXISTS
- Fix: delete from _sqlx_migrations when needed
- Fly Depot builder caches aggressively — --no-cache doesn't always bust cache
- Crashed machines need `fly machine destroy --force` then fresh `fly deploy`
